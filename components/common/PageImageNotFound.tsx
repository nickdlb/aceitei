import React from 'react';

const PageImageNotFound: React.FC = () => {
  return (
    <div className="flex h-screen items-center justify-center">
      Imagem não encontrada
    </div>
  );
};

export default PageImageNotFound;
